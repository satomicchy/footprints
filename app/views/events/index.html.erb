<h1>events</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Organize at</th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.organize_at %></td>
    <td><%= link_to 'Show', event %></td>
  </tr>
<% end %>
</table>

<br />
